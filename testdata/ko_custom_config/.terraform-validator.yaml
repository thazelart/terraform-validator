files:
  default:
    authorized_blocks:
      - variable
      - output
      - provider
      - terraform
      - module
      - data
      - locals
ensure_terraform_version: false
block_pattern_name: "^[a-z0-9-]*$"
